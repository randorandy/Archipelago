# The displayed name of the game in the website, client, etc.
import_game = "Super Zero Mission"

# The file suffix of patches for this romhack
import_patch_file_ending = ".apszm"

# The name of the world folder, also names the .apworld folder
import_apworld_name = "super_zero_mission"

# A unique index for this romhack, different from all other AP games
# Perhaps use the index on metconst times 1 million
import_base_id = 19000000

# Based on the hack, which early items are needed
import_early_items = ["Morph"]

# This heads the name in the rom which is checked to authenticate the seed
import_file_header = "SZ"